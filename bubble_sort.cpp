#include "stdc++.h"

using namespace std;

int main(int argc, char const *argv[])
{
	int n;
	cin >> n;
	int A[n];
	for (int i = 0; i < n; ++i)
	{
		cin >> A[i];
	}
	for (int i = 0; i < n; ++i)
	{
		int value = A[i];
		for (int j = 0; j < n; ++j)
		{

		}
	}
	return 0;
}
